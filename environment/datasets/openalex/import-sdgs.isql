log_enable(3,1);
SPARQL CLEAR GRAPH  <$ARGV[$I]graph/openalex-sdgs>;


DELETE FROM DB.DBA.load_list;
ld_dir ('/database/import', 'openalex-sdgs.ttl', '$ARGV[$I]graph/openalex-sdgs');

rdf_loader_run ();
checkpoint;

-- output possible load errors
SELECT * FROM DB.DBA.load_list;

SPARQL SELECT DISTINCT (<$ARGV[$I]graph/openalex-sdgs> as ?g)  (COUNT(*) as ?triples)  
WHERE { GRAPH <$ARGV[$I]graph/openalex-sdgs> {?s ?p ?o} };

exit;
